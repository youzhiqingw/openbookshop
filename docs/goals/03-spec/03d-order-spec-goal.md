# 03d Order SPEC Goal

```text
/goal create only the order SPEC for OpenBookShop until:

- read AGENTS.md, docs/specs/catalog.md, docs/PLAN.md
- define CartItem, Address, Order, OrderItem models and state transitions
- define inventory deduction at order creation and 30-minute unpaid release
- define select_for_update or equivalent transaction rules
- define Customer owner access, Merchant own-order access, and Admin all-order access
- define APIs, validation, errors, tests, migration, and rollback
- write docs/specs/order.md

Do not write implementation code.
Stop after this SPEC is complete or after 6 turns.
```

